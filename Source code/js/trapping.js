//ferver_1_choice
$(function() {
  var chk = $('.fever1_choice');
  var btn = $('.fever_click_submit_1');

  chk.on('change', function() {
    btn.prop("disabled", !this.checked);//true: disabled, false: enabled
  }).trigger('change'); //page load trigger event
  });


  //loss_appetite_choice1
  $(function() {
    var chk = $('.loss_appetite1_choice');
    var btn = $('.loss_appetite_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });


  //loss_appetite_choice2
  $(function() {
    var chk = $('.loss_appetite2_choice');
    var btn = $('.loss_appetite_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

  //loss_appetite_choice3
  $(function() {
    var chk = $('.loss_appetite3_choice');
    var btn = $('.loss_appetite_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
  //loss_appetite_choice4
  $(function() {
    var chk = $('.loss_appetite4_choice');
    var btn = $('.loss_appetite_click_submit_4');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
  //roe1_choice
  $(function() {
    var chk = $('.roe1_choice');
    var btn = $('.roe_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

  //coughing1_choice
  $(function() {
    var chk = $('.coughing1_choice');
    var btn = $('.coughing_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
   //coughing2_choice
   $(function() {
    var chk = $('.coughing2_choice');
    var btn = $('.coughing_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
  //coughing3_choice
  $(function() {
    var chk = $('.coughing3_choice');
    var btn = $('.coughing_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
  //coughing4_choice
  $(function() {
    var chk = $('.coughing4_choice');
    var btn = $('.coughing_click_submit_4');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

  //diarrhea1_choice
  $(function() {
    var chk = $('.diarrhea1_choice');
    var btn = $('.diarrhea_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });
  
   //diarrhea2_choice
   $(function() {
    var chk = $('.diarrhea2_choice');
    var btn = $('.diarrhea_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

   //diarrhea3_choice
   $(function() {
    var chk = $('.diarrhea3_choice');
    var btn = $('.diarrhea_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //lwf1_choice
   $(function() {
    var chk = $('.lwf1_choice');
    var btn = $('.lwf_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //bloody_diarrhea1_choice
   $(function() {
    var chk = $('.bloody_diarrhea1_choice');
    var btn = $('.bloody_diarrhea_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //weight_loss1_choice
   $(function() {
    var chk = $('.weight_loss1_choice');
    var btn = $('.weight_loss_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //weight_loss2_choice
   $(function() {
    var chk = $('.weight_loss2_choice');
    var btn = $('.weight_loss_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //weight_loss3_choice
   $(function() {
    var chk = $('.weight_loss3_choice');
    var btn = $('.weight_loss_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //weight_loss4_choice
   $(function() {
    var chk = $('.weight_loss4_choice');
    var btn = $('.weight_loss_click_submit_4');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //weight_loss5_choice
   $(function() {
    var chk = $('.weight_loss5_choice');
    var btn = $('.weight_loss_click_submit_5');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

     //weight_loss6_choice
   $(function() {
    var chk = $('.weight_loss6_choice');
    var btn = $('.weight_loss_click_submit_6');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth1_choice
    $(function() {
    var chk = $('.slow_growth1_choice');
    var btn = $('.slow_growth_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth2_choice
    $(function() {
    var chk = $('.slow_growth2_choice');
    var btn = $('.slow_growth_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth3_choice
    $(function() {
    var chk = $('.slow_growth3_choice');
    var btn = $('.slow_growth_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth4_choice
   $(function() {
    var chk = $('.slow_growth4_choice');
    var btn = $('.slow_growth_click_submit_4');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth5_choice
   $(function() {
    var chk = $('.slow_growth5_choice');
    var btn = $('.slow_growth_click_submit_5');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //slow_growth6_choice
   $(function() {
    var chk = $('.slow_growth6_choice');
    var btn = $('.slow_growth_click_submit_6');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

     //trembling1_choice
   $(function() {
    var chk = $('.trembling1_choice');
    var btn = $('.trembling_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //trembling2_choice
   $(function() {
    var chk = $('.trembling2_choice');
    var btn = $('.trembling_click_submit_2');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //trembling3_choice
   $(function() {
    var chk = $('.trembling3_choice');
    var btn = $('.trembling_click_submit_3');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

   //trembling4_choice
   $(function() {
    var chk = $('.trembling4_choice');
    var btn = $('.trembling_click_submit_4');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });

    //trembling4_choice
   $(function() {
    var chk = $('.vomiting1_choice');
    var btn = $('.vomiting_click_submit_1');
  
    chk.on('change', function() {
      btn.prop("disabled", !this.checked);//true: disabled, false: enabled
    }).trigger('change'); //page load trigger event
    });





  
  
  
  
